---
title: "Implementation Tasks: In-App Notifications & Activity Feed"
---

# Implementation Tasks: In-App Notifications & Activity Feed

**Epic**: 024-NTF
**Plan**: [plan.md](/initiatives/FL-financial-ledger/024-NTF-notifications-activity-feed/plan)
**Mode**: AI
**Generated**: 2026-03-15

---

## Phase 1 — Backend Foundation

- [X] T001 [US1] Enum: `NotificationType` backed string enum. File: `app/Enums/NotificationType.php`. Cases: `JeSubmitted = 'je_submitted'`, `JeApproved = 'je_approved'`, `JeRejected = 'je_rejected'`, `InvoiceSent = 'invoice_sent'`, `InvoicePaid = 'invoice_paid'`, `InvoiceOverdue = 'invoice_overdue'`, `JobShareViewed = 'job_share_viewed'`, `BankFeedSynced = 'bank_feed_synced'`, `BankFeedError = 'bank_feed_error'`, `ReconciliationCompleted = 'reconciliation_completed'`. Methods: `label(): string` (human readable), `icon(): string` (heroicon name: e.g. `check-circle`, `x-circle`, `bell`, `document-check`, `banknotes`, `exclamation-triangle`, `eye`, `arrow-path`, `arrow-path-rounded-square`), `filterCategory(): string` (returns one of: `'Approvals'`, `'Invoicing'`, `'Banking'`, `'Jobs'`, `'All'` — JeSubmitted/Approved/Rejected → Approvals; Invoice* → Invoicing; BankFeed*/ReconciliationCompleted → Banking; JobShareViewed → Jobs).

- [X] T002 [US1] Migration: `create_notifications_table`. File: `database/migrations/2026_03_15_200001_create_notifications_table.php`. Columns: `id` bigIncrements, `uuid` uuid unique, `workspace_id` unsignedBigInteger not null, `user_id` unsignedBigInteger not null, `type` varchar(50) not null, `title` varchar(255) not null, `body` text not null, `subject_type` varchar(100) nullable, `subject_id` unsignedBigInteger nullable, `actor_id` unsignedBigInteger nullable, `read_at` timestamp nullable, `dismissed_at` timestamp nullable, `created_at`+`updated_at` timestamps. Indexes: composite `(workspace_id, user_id, read_at)`, composite `(workspace_id, user_id, dismissed_at, created_at)`, composite `(subject_type, subject_id)`, composite `(type, workspace_id)`. Foreign keys: `workspace_id` → `workspaces.id` onDelete cascade, `user_id` → `users.id` onDelete cascade, `actor_id` → `users.id` onDelete set null.

- [X] T003 [US1] Model: `Notification`. File: `app/Models/Tenant/Notification.php`. Traits: `HasUuids`. Table: `notifications`. `$fillable`: `['workspace_id','user_id','type','title','body','subject_type','subject_id','actor_id','read_at','dismissed_at']`. `$casts`: `['type' => NotificationType::class, 'read_at' => 'datetime', 'dismissed_at' => 'datetime']`. Relationships: `user(): BelongsTo User`, `workspace(): BelongsTo Workspace`, `actor(): BelongsTo User` (foreignKey `actor_id`), `subject(): MorphTo`. Scopes: `scopeUnread($query)` → `whereNull('read_at')`, `scopeActive($query)` → `whereNull('dismissed_at')`, `scopeForWorkspace($query, int $workspaceId)` → `where('workspace_id', $workspaceId)`. No global scope — workspace filtering done explicitly in controller. Register `'notification'` → `Notification::class` in the `Relation::morphMap()` array inside `app/Providers/AppServiceProvider.php` boot().

- [X] T004 [US1,US7] Action: `CreateNotification`. File: `app/Actions/Notifications/CreateNotification.php`. Use `AsAction` trait. Method signature: `handle(int $workspaceId, int $userId, NotificationType $type, string $title, string $body, ?object $subject = null, ?int $actorId = null): Notification`. Creates and returns a `Notification` record. If `$subject` is provided, set `subject_type` to the morph alias (use `Relation::getMorphedModel()` or morph map lookup) and `subject_id` to `$subject->id`. Never throws — if creation fails, log with `Log::error()` and return a new unsaved `Notification` instance. This ensures listener failures don't block domain operations.

- [X] T005 [US1] Enrich `InvoiceSent` event: Add `sentBy: int` constructor parameter. File: `app/Events/Invoicing/InvoiceSent.php`. Update `InvoiceAggregate::markSent(int $sentBy)` in `app/Aggregates/InvoiceAggregate.php` to accept and pass `sentBy`. Update `app/Actions/Invoicing/SendInvoice.php` to accept and pass `$sentBy` (user ID). Update `InvoiceController::send()` in `app/Http/Controllers/Api/InvoiceController.php` to pass `$request->user()->id` to `SendInvoice::run()`. Run existing invoice tests to confirm nothing breaks: `php artisan test --filter=Invoice --compact`.

- [X] T006 [US1] Resource: `NotificationResource`. File: `app/Http/Resources/NotificationResource.php`. `toArray()` returns: `uuid`, `type` (enum value string), `title`, `body`, `icon` (`$this->type->icon()`), `filter_category` (`$this->type->filterCategory()`), `subject_url` (build from subject_type + subject_id: `'journal_entry'` → `/journal-entries/{uuid}`, `'invoice'` → `/invoices/{uuid}`, `'job'` → `/jobs/{uuid}`, `'bank_account'` → `/banking/{id}` — resolve uuid by querying model only if subject_id is set), `actor` (nullable: `['name' => $this->actor?->name]`), `read_at` (ISO8601 or null), `dismissed_at` (ISO8601 or null), `created_at` (ISO8601), `relative_time` (use Carbon `$this->created_at->diffForHumans()`). Always load actor relation to avoid N+1 (controller must eager-load).

- [X] T007 [US1,US2,US7] Controller: `NotificationController`. File: `app/Http/Controllers/Api/NotificationController.php`. Six methods: (1) `unreadCount(Request $request): JsonResponse` — returns `['data' => ['count' => Notification::forWorkspace($wsId)->active()->unread()->count()]]`. (2) `recent(Request $request): AnonymousResourceCollection` — returns top 10 `active()` notifications for workspace, ordered by `created_at` desc, with `actor` eager-loaded, wrapped in `NotificationResource::collection()`. (3) `index(Request $request): AnonymousResourceCollection` — returns `active()` notifications for workspace, ordered by `created_at` desc, paginated 25/page, supports `?type=` filter (valid NotificationType value), with `actor` eager-loaded. (4) `markAllRead(Request $request): JsonResponse` — sets `read_at = now()` for all `active()->unread()` notifications in workspace; returns `['message' => 'All notifications marked as read.']`. (5) `markRead(Request $request, Notification $notification): JsonResponse` — sets `read_at = now()` if null; verify `$notification->workspace_id === $wsId` and `$notification->user_id === $request->user()->id` else `abort(403)`; returns `['message' => 'Notification marked as read.']`. (6) `dismiss(Request $request, Notification $notification): JsonResponse` — sets `dismissed_at = now()`; same ownership checks as markRead; returns `['message' => 'Notification dismissed.']`.

- [X] T008 [US1,US2,US7] Routes: Register notification routes in `routes/api.php` inside the workspace middleware group. Add import for `NotificationController`. Routes: `GET notifications/unread-count` → `unreadCount`, `GET notifications/recent` → `recent`, `GET notifications` → `index`, `POST notifications/mark-all-read` → `markAllRead`, `PATCH notifications/{notification}/read` → `markRead` (model binding by UUID), `DELETE notifications/{notification}` → `dismiss` (model binding by UUID). All within `middleware(['auth:sanctum', SetWorkspaceContext::class])` group. Route model binding resolves `Notification` by `uuid` column — ensure `getRouteKeyName()` returns `'uuid'` on the model.

- [X] T009 [US1,US2,US7] Feature tests: File `tests/Feature/Api/NotificationControllerTest.php`. Use `RefreshDatabase`, seed `RolesAndPermissionsSeeder`, create workspace + owner user. Tests: (1) unread-count returns 0 with no notifications; (2) unread-count returns correct count with seeded unread notifications; (3) recent returns max 10, ordered desc; (4) index paginates at 25 per page; (5) index filters by `?type=je_submitted`; (6) index excludes dismissed notifications; (7) markAllRead sets read_at on all unread; (8) markRead sets read_at on single; (9) dismiss sets dismissed_at; (10) cannot dismiss another user's notification (403); (11) unread-count excludes dismissed. ~11 assertions total.

---

## Phase 2 — Event Listeners

- [X] T010 [P] [US3] Listener: `NotifyApproversOnJeSubmitted`. File: `app/Listeners/Notifications/NotifyApproversOnJeSubmitted.php`. Listens to `App\Events\Ledger\JournalEntrySubmitted`. `handle(JournalEntrySubmitted $event)`: (1) resolve JournalEntry by `$event->aggregateRootUuid()`: `JournalEntry::where('workspace_id', $event->workspaceId)->where('uuid', $event->aggregateRootUuid())->first()` — return early if null. (2) Get all workspace users with approver role: `$workspace->users()->role('approver')->get()`. (3) Exclude submitter: filter out `$event->submittedBy`. (4) For each remaining user call `CreateNotification::run(workspaceId: $event->workspaceId, userId: $user->id, type: NotificationType::JeSubmitted, title: 'Journal entry awaiting approval', body: "JE #{$je->reference} submitted by {$submitter->name}", subject: $je, actorId: $event->submittedBy)`. Wrap entire handle in try/catch, `Log::error()` on exception.

- [X] T011 [P] [US3] Listener: `NotifyBookkeeperOnJeApproved`. File: `app/Listeners/Notifications/NotifyBookkeeperOnJeApproved.php`. Listens to `App\Events\Ledger\JournalEntryApproved`. `handle(JournalEntryApproved $event)`: (1) Resolve JournalEntry by `$event->aggregateRootUuid()`. (2) Resolve submitter: `User::find($je->created_by)` — return early if null. (3) Resolve approver: `User::find($event->approvedBy)`. (4) Call `CreateNotification::run(workspaceId: $event->workspaceId, userId: $je->created_by, type: NotificationType::JeApproved, title: 'Journal entry approved', body: "JE #{$je->reference} approved by {$approver->name}", subject: $je, actorId: $event->approvedBy)`. Wrap in try/catch.

- [X] T012 [P] [US3] Listener: `NotifyBookkeeperOnJeRejected`. File: `app/Listeners/Notifications/NotifyBookkeeperOnJeRejected.php`. Listens to `App\Events\Ledger\JournalEntryRejected`. `handle(JournalEntryRejected $event)`: (1) Resolve JournalEntry. (2) Build body: `"JE #{$je->reference} rejected by {$rejector->name}"` + if `$event->reason` is non-empty append `". Reason: {$event->reason}"`. (3) Call `CreateNotification::run(type: NotificationType::JeRejected, ...)`. Wrap in try/catch.

- [X] T013 [P] [US4] Listener: `NotifyInvoiceSender`. File: `app/Listeners/Notifications/NotifyInvoiceSender.php`. Listens to `App\Events\Invoicing\InvoiceSent`. `handle(InvoiceSent $event)`: (1) Resolve Invoice via `$event->aggregateRootUuid()`. (2) Use `$event->sentBy` (added in T005) to identify recipient. (3) Resolve contact name: `$invoice->contact->name`. (4) `CreateNotification::run(userId: $event->sentBy, type: NotificationType::InvoiceSent, title: "Invoice #{$invoice->invoice_number} sent", body: "Invoice #{$invoice->invoice_number} sent to {$contactName}", subject: $invoice, actorId: $event->sentBy)`. Wrap in try/catch.

- [X] T014 [P] [US4] Listener: `NotifyOwnersOnInvoicePaid`. File: `app/Listeners/Notifications/NotifyOwnersOnInvoicePaid.php`. Listens to `App\Events\Invoicing\InvoicePaymentReceived`. `handle(InvoicePaymentReceived $event)`: (1) Resolve Invoice. (2) Format amount: `'$' . number_format($event->amount / 100, 2)`. (3) Build recipients: get all workspace users with `owner` or `accountant` role. (4) If `$invoice->created_by` is not already in that set (i.e. they are a bookkeeper), add them. (5) De-dup by user ID using `collect()->unique('id')`. (6) For each recipient: `CreateNotification::run(type: NotificationType::InvoicePaid, title: "Invoice #{$invoice->invoice_number} paid", body: "{$amount} received from {$invoice->contact->name}", subject: $invoice, actorId: $event->recordedBy)`. Wrap in try/catch.

- [X] T015 [P] [US5] Listener: `NotifyOnJobShareViewed`. File: `app/Listeners/Notifications/NotifyOnJobShareViewed.php`. This is a **stub** — the `JobShareLinkViewed` event from 022-CPV does not exist on this branch. Use `class_exists('App\Events\Jobs\JobShareLinkViewed')` check. If it doesn't exist, log a debug note and return. When CPV ships, this listener will: (1) check if a notification was already created for this token in the last 24 hours UTC window (query `Notification` where `subject_type = 'job_share_token'` and `subject_id = $token->id` and `created_at >= today midnight UTC`) — if exists, return. (2) Otherwise `CreateNotification::run(userId: $token->created_by, type: NotificationType::JobShareViewed, title: "Client viewed {$job->name}", body: "Your client opened the {$job->name} dashboard", subject: $token, actorId: null)`. Register the listener binding in `EventServiceProvider` as a comment block: `// 'App\Events\Jobs\JobShareLinkViewed' => [NotifyOnJobShareViewed::class],`. Wrap in try/catch.

- [X] T016 [P] [US6] Listener: `NotifyOwnersOnBankFeedSynced`. File: `app/Listeners/Notifications/NotifyOwnersOnBankFeedSynced.php`. This listener is **not** triggered by a stored event — it is called directly from `app/Actions/Banking/SyncBankFeed.php` after a successful import. Update `SyncBankFeed::handle()` to call `NotifyOwnersOnBankFeedSynced::run($bankAccount, $result)` after `$bankAccount->update(['last_synced_at' => now()])`. The listener/action: gets workspace users with `owner` or `accountant` role from `$bankAccount->workspace`. Builds body: if `$result['imported'] === 0` → `"Bank feed synced — no new transactions."`, else if `$result['unmatched'] === 0` → `"Bank feed synced — all {$result['imported']} transactions matched."`, else → `"Bank feed synced — {$result['imported']} new transactions, {$result['unmatched']} unmatched."`. Calls `CreateNotification::run()` per recipient with `type: NotificationType::BankFeedSynced`, `subject: $bankAccount`. Note: `SyncBankFeed` returns `['imported', 'skipped', 'total']` — treat `skipped` as unmatched for the message. Use `AsAction` trait on the listener class so it can be called via `::run()`.

- [X] T017 [P] [US6] Update `SyncBankFeed` error handling: Wrap the call to `$provider->fetchTransactions()` and `ImportBankTransactions::run()` in a try/catch in `SyncBankFeed::handle()`. On exception: call `NotifyOwnersOnBankFeedError::run($bankAccount, $e->getMessage())` then re-throw. File: `app/Actions/Banking/SyncBankFeed.php`. Create `app/Listeners/Notifications/NotifyOwnersOnBankFeedError.php` with `AsAction`. Gets workspace users with `owner` or `accountant` role. `CreateNotification::run(type: NotificationType::BankFeedError, title: 'Bank feed sync failed', body: "Bank feed sync failed — {$bankAccount->name}. Check your bank feed connection.", subject: $bankAccount, actorId: null)` per recipient.

- [X] T018 Register all listeners in `EventServiceProvider`. File: `app/Providers/EventServiceProvider.php` (or if not present, create it and register in `bootstrap/providers.php`). Add to `$listen` array: `JournalEntrySubmitted::class => [NotifyApproversOnJeSubmitted::class]`, `JournalEntryApproved::class => [NotifyBookkeeperOnJeApproved::class]`, `JournalEntryRejected::class => [NotifyBookkeeperOnJeRejected::class]`, `InvoiceSent::class => [NotifyInvoiceSender::class]`, `InvoicePaymentReceived::class => [NotifyOwnersOnInvoicePaid::class]`. Comment stub: `// 'App\Events\Jobs\JobShareLinkViewed' => [NotifyOnJobShareViewed::class],`. Note: BankFeed listeners are called directly from `SyncBankFeed`, not via the event system.

- [X] T019 [P] [US4] Migration: Add `overdue_notified_at` to invoices. File: `database/migrations/2026_03_15_200002_add_overdue_notified_at_to_invoices.php`. `$table->timestamp('overdue_notified_at')->nullable()->after('voided_at')`. Add `'overdue_notified_at'` to `Invoice` model `$fillable` and cast as `'datetime'`.

- [X] T020 [US4] Artisan command: `SendInvoiceOverdueNotifications`. File: `app/Console/Commands/SendInvoiceOverdueNotifications.php`. Signature: `notifications:send-overdue`. `handle()`: query all invoices where `due_date < today`, `amount_due > 0`, `status IN ('sent','partially_paid','overdue')`, and `overdue_notified_at IS NULL`. For each invoice: get workspace users with `owner` or `accountant` role. For each: `CreateNotification::run(type: NotificationType::InvoiceOverdue, title: "Invoice #{$invoice->invoice_number} is overdue", body: "$" . number_format($invoice->amount_due / 100, 2) . " outstanding from {$invoice->contact->name}", subject: $invoice, actorId: null)`. After notifications created, set `$invoice->update(['overdue_notified_at' => now()])`. Wrap each invoice processing in try/catch — one invoice failure must not stop others. Output `$this->info("Sent overdue notification for invoice #{$invoice->invoice_number}")` per invoice.

- [X] T021 [P] Feature tests for listeners. File: `tests/Feature/Notifications/`. Four test files: (a) `JeNotificationsTest.php`: seed JournalEntry in `awaiting_approval` status, fire `JournalEntrySubmitted` event via `event(new JournalEntrySubmitted(...))`, assert `Notification` records created for all approvers but NOT for submitter. Fire `JournalEntryApproved`, assert notification for bookkeeper. Fire `JournalEntryRejected` with reason, assert notification contains reason string. (b) `InvoiceNotificationsTest.php`: fire `InvoiceSent`, assert notification for sender. Fire `InvoicePaymentReceived`, assert notifications for all owners/accountants + bookkeeper raiser, no duplicates. (c) `BankFeedNotificationsTest.php`: call `NotifyOwnersOnBankFeedSynced::run($bankAccount, ['imported' => 5, 'skipped' => 2, 'total' => 7])`, assert notification body contains "5 new transactions, 2 unmatched". Call `NotifyOwnersOnBankFeedError::run($bankAccount, 'timeout')`, assert notification created. (d) `OverdueNotificationsCommandTest.php`: seed overdue invoice with `overdue_notified_at = null`, run `$this->artisan('notifications:send-overdue')`, assert notification created and `overdue_notified_at` set. Run again, assert no second notification.

---

## Phase 3 — Scheduled Commands & Purge

- [X] T022 [P] [US2] Artisan command: `PurgeOldNotifications`. File: `app/Console/Commands/PurgeOldNotifications.php`. Signature: `notifications:purge`. `handle()`: delete all notifications where `created_at < now()->subDays(90)`. Use chunked delete to avoid table lock: `Notification::where('created_at', '<', now()->subDays(90))->chunkById(500, fn ($chunk) => $chunk->each->delete())`. Output count of deleted records.

- [X] T023 Schedule both commands. In `routes/console.php` (Laravel 12 style) or `app/Console/Kernel.php`: `Schedule::command('notifications:send-overdue')->dailyAt('06:00')->timezone('UTC')`. `Schedule::command('notifications:purge')->dailyAt('03:00')->timezone('UTC')`.

- [X] T024 [P] Feature test: `PurgeNotificationsCommandTest.php`. Seed one notification with `created_at = now()->subDays(91)` and one with `created_at = now()->subDays(30)`. Run `$this->artisan('notifications:purge')`. Assert old notification deleted, recent notification intact.

---

## Phase 4 — Frontend

- [X] T025 [P] Types: File `frontend/src/types/notifications.ts`. Export: `NotificationType` string union type (`'je_submitted' | 'je_approved' | 'je_rejected' | 'invoice_sent' | 'invoice_paid' | 'invoice_overdue' | 'job_share_viewed' | 'bank_feed_synced' | 'bank_feed_error' | 'reconciliation_completed'`). Export `FilterCategory` union: `'All' | 'Approvals' | 'Invoicing' | 'Banking' | 'Jobs'`. Export `Notification` interface: `{ uuid: string; type: NotificationType; title: string; body: string; icon: string; filter_category: FilterCategory; subject_url: string | null; actor: { name: string } | null; read_at: string | null; dismissed_at: string | null; created_at: string; relative_time: string }`. Export `PaginatedNotifications` interface with Laravel pagination shape: `{ data: Notification[]; current_page: number; last_page: number; per_page: number; total: number }`.

- [X] T026 [P] [US1] Zustand store: `frontend/src/stores/notification.ts`. `useNotificationStore` with state: `isOpen: boolean`, actions: `setOpen(open: boolean): void`, `toggle(): void`. No persistence. Export as named hook.

- [X] T027 [P] [US1] Hook: `frontend/src/hooks/use-unread-count.ts`. TanStack Query `useQuery` fetching `GET /api/v1/notifications/unread-count` with workspace header. Options: `queryKey: ['notifications', 'unread-count', workspaceId]`, `refetchInterval: 30_000`, `select: (data) => data.data.count as number`. Uses `useWorkspaceId()` composable (or reads from workspace store). Returns `{ count: number; isLoading: boolean }`.

- [X] T028 [P] [US1] Hook: `frontend/src/hooks/use-recent-notifications.ts`. TanStack Query `useQuery` fetching `GET /api/v1/notifications/recent` with workspace header. `queryKey: ['notifications', 'recent', workspaceId]`. `enabled: isOpen` (only fetches when bell is open, passed as parameter). Returns `{ notifications: Notification[]; isLoading: boolean }`. On success, call `useMutation` for `POST /api/v1/notifications/mark-all-read` automatically (auto-mark-read on open) then `queryClient.invalidateQueries(['notifications', 'unread-count', workspaceId])`.

- [X] T029 [P] [US2] Hook: `frontend/src/hooks/use-notifications.ts`. TanStack Query `useQuery` with `queryKey: ['notifications', 'feed', workspaceId, type]`. Fetches `GET /api/v1/notifications?page={page}&type={type}` (type omitted when `'All'`). Returns `{ notifications: Notification[]; pagination: { currentPage, lastPage, total }; isLoading: boolean }`. Export `useMarkAllRead()` mutation: `POST /api/v1/notifications/mark-all-read`, on success invalidate `unread-count` and `feed` queries. Export `useDismissNotification()` mutation: `DELETE /api/v1/notifications/{uuid}`, on success remove from feed cache optimistically using `queryClient.setQueryData`.

- [X] T030 [P] [US1] Component: `frontend/src/components/notifications/notification-item.tsx`. `'use client'`. Props: `{ notification: Notification; onDismiss?: (uuid: string) => void; showDismiss?: boolean }`. Renders: left icon (use shadcn `Badge` or inline SVG based on `notification.icon`), right side with title (bold if `read_at === null`), body (truncated to 2 lines, `line-clamp-2`), relative_time (muted xs text). Unread indicator: blue dot left border or left accent bar (`border-l-2 border-blue-500`) when `read_at === null`. If `showDismiss` and `onDismiss`: render `X` icon button top-right. Entire row is a Next.js `<Link href={notification.subject_url ?? '#'}>` — if `subject_url` is null, render as `<div>` instead. TypeScript: no `any`.

- [X] T031 [US1] Component: `frontend/src/components/notifications/notification-bell.tsx`. `'use client'`. Uses `useUnreadCount()`, `useNotificationStore()`. Renders shadcn `Popover` with trigger: Bell icon (`lucide-react` `Bell`) button. Badge: if count > 0, render red `Badge` positioned absolute top-right of bell. Display `count > 99 ? '99+' : count`. On `onOpenChange`: call `store.setOpen()`. No keyboard shortcut bound to bell — `Cmd+/` is reserved for AI panel. Include tooltip: `"Notifications"`.

- [X] T032 [US1] Component: `frontend/src/components/notifications/notification-dropdown.tsx`. `'use client'`. Uses `useRecentNotifications(isOpen)`, `useMarkAllRead()`, `useNotificationStore()`. Renders inside `PopoverContent` (width `w-96`). Header: "Notifications" title + "Mark all read" button (calls `markAllRead` mutation). Body: if loading → skeleton list. If empty → centered text "No notifications yet." with `Bell` icon. Else → list of `<NotificationItem>` (showDismiss=false). Footer: `<Link href="/notifications">View all notifications →</Link>`. On close (isOpen → false): no additional action needed (auto-mark triggered on open in T028).

- [X] T033 [US2] Component: `frontend/src/components/notifications/notification-type-filter.tsx`. `'use client'`. Props: `{ value: FilterCategory; onChange: (cat: FilterCategory) => void }`. Renders shadcn `Tabs` with values: `'All'`, `'Approvals'`, `'Invoicing'`, `'Banking'`, `'Jobs'`. On tab change call `onChange`. TypeScript strict.

- [X] T034 [US2,US7] Component: `frontend/src/components/notifications/notification-feed.tsx`. `'use client'`. Props: `{ workspaceId: number }`. Uses `useNotifications()`, `useDismissNotification()`, local state for `page: number` and `type: FilterCategory`. Renders: `NotificationTypeFilter` at top. If loading → skeleton. If empty → empty state with heading "No activity yet" and sub-line "Notifications will appear here when workspace events occur." Else → list of `<NotificationItem showDismiss onDismiss={dismiss}>`. Pagination: prev/next buttons using `currentPage`/`lastPage`.

- [X] T035 [US2,US7] Page: `frontend/src/app/(dashboard)/notifications/page.tsx`. Server component wrapper (no `'use client'`). Renders `<PageHeader title="Notifications" />` and `<NotificationFeed workspaceId={...} />` (client island). "Mark all as read" button in page header actions area calling `useMarkAllRead()` mutation. Add `G then N` navigation shortcut — update `useKeyboardShortcuts` hook to include `{ keys: ['g', 'n'], action: () => router.push('/notifications'), description: 'Go to Notifications' }`. Update `CLAUDE.md` keyboard shortcuts table to add `G then N` → `Go to Notifications`.

- [X] T036 [US1] Wire bell into layout: `frontend/src/app/(dashboard)/layout.tsx`. Import `NotificationBell` (dynamic import with `ssr: false` to avoid hydration mismatch). Place bell in the header `<SidebarTrigger>` area, right of workspace switcher, left of user menu. Wrap `NotificationBell` in `<Suspense fallback={<Bell className="h-5 w-5 opacity-30" />}>`.

- [X] T037 [P] Add `"Notifications"` entry to sidebar navigation in `frontend/src/components/app-sidebar.tsx` (or wherever nav items are defined). Item: `{ title: 'Notifications', href: '/notifications', icon: Bell, shortcut: 'G then N' }`. Render `kbd` badges per CLAUDE.md convention. Place below "Journal Entries" or in a dedicated "Activity" section.

---

## Phase 5 — Browser Tests

- [X] T038 [US1,US2,US7] Browser test file: `tests/Browser/NotificationsTest.php`. Pest Playwright tests. Setup: `browserLogin()` as admin, seed 3 `Notification` records for the logged-in user via `Notification::factory()->create([...])` (create factory in T039). Tests: (1) `it_shows_bell_icon_in_header` — visit dashboard, assert bell icon visible. (2) `it_shows_unread_badge_when_notifications_exist` — seed unread notification, visit dashboard, assert badge visible with count. (3) `it_opens_dropdown_on_bell_click` — click bell, assert dropdown visible with notification title. (4) `it_navigates_to_notifications_page_via_view_all` — open bell, click "View all notifications", assert URL is `/notifications`. (5) `it_renders_activity_feed_page` — visit `/notifications`, assert heading "Notifications" visible. (6) `it_shows_filter_tabs_on_feed` — visit `/notifications`, assert tabs "All", "Approvals", "Invoicing", "Banking", "Jobs" visible. (7) `it_shows_empty_state_when_no_notifications` — visit `/notifications` with no seeded data, assert "No activity yet" text.

- [X] T039 [P] Factory: `database/factories/NotificationFactory.php`. Extends `Factory`. Model: `Notification`. `definition()`: `['workspace_id' => 1, 'user_id' => 1, 'type' => NotificationType::JeApproved, 'title' => 'Journal entry approved', 'body' => 'JE-001 approved by Sarah Chen', 'read_at' => null, 'dismissed_at' => null]`. States: `unread()` → `['read_at' => null]`, `read()` → `['read_at' => now()]`.

---

## Phase 6 — Quality

- [X] T040 [P] Run `vendor/bin/pint --dirty` on all new PHP files. Fix any formatting issues.

- [X] T041 [P] Run full test suite: `php artisan test --compact`. Confirm all existing tests still pass after `InvoiceSent` event enrichment (T005) and `SyncBankFeed` changes (T016, T017). Fix any regressions.

- [X] T042 [P] TypeScript check: `cd frontend && npx tsc --noEmit`. Fix any type errors introduced by new notification types and components.

---

## Summary

| Phase | Tasks | Parallelizable | Key Output |
|-------|-------|----------------|------------|
| 1 — Backend Foundation | T001–T009 | T001–T006 parallel, T007 needs T003+T006, T008 needs T007 | Notification model, CRUD API |
| 2 — Event Listeners | T010–T021 | T010–T017 parallel | 7 listeners + overdue command |
| 3 — Scheduled Commands | T022–T024 | T022–T024 parallel | Purge + schedule |
| 4 — Frontend | T025–T037 | T025–T029 parallel, T030–T037 sequential | Bell, dropdown, feed page |
| 5 — Browser Tests | T038–T039 | T039 before T038 | 7 browser scenarios |
| 6 — Quality | T040–T042 | All parallel | Pint, tests, TypeScript |

**Total tasks**: 42
**~25 feature/unit tests** + **7 browser tests**
**MVP (P1 stories)**: T001–T012, T018–T019, T025–T036 — covers bell, feed, JE approval workflow notifications
