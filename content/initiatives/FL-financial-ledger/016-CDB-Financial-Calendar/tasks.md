---
title: "Implementation Tasks: Financial Calendar & Meetings"
---

# Implementation Tasks: Financial Calendar & Meetings

**Plan**: [plan.md](plan.md) | **Spec**: [spec.md](spec.md) | **Generated**: 2026-03-19
**Mode**: AI (agent-executable)

---

## Phase 1: Foundation — Migrations, Enums, Models, Permissions

- [ ] T001 Migration: `create_meetings_table` — columns: id (bigIncrements), uuid (uuid, unique), workspace_id (foreignId → workspaces), title (string 255), type (string — MeetingType enum), starts_at (datetime), ends_at (datetime), contact_id (foreignId → contacts, nullable, nullOnDelete), location (string 500, nullable), description (text, nullable), status (string — MeetingStatus enum, default 'scheduled'), created_by (foreignId → users), recurring_template_id (foreignId → recurring_templates, nullable, nullOnDelete), completed_at (datetime, nullable), cancelled_at (datetime, nullable), timestamps, softDeletes. Indexes: (workspace_id, starts_at), (workspace_id, status), (contact_id), (recurring_template_id). File: `database/migrations/xxxx_create_meetings_table.php`
- [ ] T002 Migration: `create_meeting_attendees_table` — columns: id (bigIncrements), meeting_id (foreignId → meetings, cascadeOnDelete), user_id (foreignId → users, nullable), email (string, nullable), name (string, nullable), rsvp_status (string — AttendeeRsvpStatus enum, default 'pending'), is_organiser (boolean, default false), invited_at (datetime), responded_at (datetime, nullable), timestamps. Unique constraints: (meeting_id, user_id) where user_id not null, (meeting_id, email) where email not null. File: `database/migrations/xxxx_create_meeting_attendees_table.php`
- [ ] T003 [P] Enum: `MeetingType` — cases: Review='review', Advisory='advisory', TaxPlanning='tax_planning', Onboarding='onboarding', General='general'. Add `label(): string` method. File: `app/Enums/MeetingType.php`
- [ ] T004 [P] Enum: `MeetingStatus` — cases: Scheduled='scheduled', Completed='completed', Cancelled='cancelled'. Add `label(): string` method. File: `app/Enums/MeetingStatus.php`
- [ ] T005 [P] Enum: `AttendeeRsvpStatus` — cases: Pending='pending', Accepted='accepted', Declined='declined'. Add `label(): string` method. File: `app/Enums/AttendeeRsvpStatus.php`
- [ ] T006 Model: `Meeting` — fillable: workspace_id, title, type, starts_at, ends_at, contact_id, location, description, status, created_by, recurring_template_id, completed_at, cancelled_at. Casts: type → MeetingType, status → MeetingStatus, starts_at → datetime, ends_at → datetime, completed_at → datetime, cancelled_at → datetime. Traits: HasAttachments, HasNotes, SoftDeletes. Relations: workspace() BelongsTo, contact() BelongsTo (nullable), creator() BelongsTo(User, 'created_by'), recurringTemplate() BelongsTo (nullable), attendees() HasMany(MeetingAttendee). Auto-generate UUID on creating event. File: `app/Models/Tenant/Meeting.php`
- [ ] T007 Model: `MeetingAttendee` — fillable: meeting_id, user_id, email, name, rsvp_status, is_organiser, invited_at, responded_at. Casts: rsvp_status → AttendeeRsvpStatus, is_organiser → boolean, invited_at → datetime, responded_at → datetime. Relations: meeting() BelongsTo, user() BelongsTo (nullable). File: `app/Models/Tenant/MeetingAttendee.php`
- [ ] T008 Update `RecurringTemplateType` enum — add `Meeting = 'meeting'` case and update `label()` method. File: `app/Enums/RecurringTemplateType.php`
- [ ] T009 Update `AppServiceProvider` — add `'meeting' => Meeting::class` to `Relation::morphMap()` array. Add `Gate::policy(Meeting::class, MeetingPolicy::class)` in `boot()`. File: `app/Providers/AppServiceProvider.php`
- [ ] T010 Policy: `MeetingPolicy` — methods: viewAny($user) → `$user->hasPermissionTo('meeting.view')`, view($user, $meeting) → same, create($user) → `$user->hasPermissionTo('meeting.create')`, update($user, $meeting) → `$user->hasPermissionTo('meeting.update')`, delete($user, $meeting) → `$user->hasPermissionTo('meeting.delete')`. File: `app/Policies/MeetingPolicy.php`
- [ ] T011 Update `RolesAndPermissionsSeeder` — add permissions: `meeting.view`, `meeting.create`, `meeting.update`, `meeting.delete`. Role assignments: owner + accountant → all 4; bookkeeper → view, create, update; approver → view; auditor → view; client → view. File: `database/seeders/RolesAndPermissionsSeeder.php`
- [ ] T012 Tests: Phase 1 foundation — test Meeting model relationships (workspace, contact, creator, recurringTemplate, attendees), test MeetingAttendee model relationships (meeting, user), test enum label() methods for all 3 enums, test MeetingPolicy authorization per role (seed RolesAndPermissionsSeeder, verify owner can CRUD, client can only view, bookkeeper can view/create/update but not delete). File: `tests/Feature/Api/MeetingFoundationTest.php`

---

## Phase 2: Meeting CRUD — Form Requests, Actions, Controllers, Routes

- [ ] T013 FormRequest: `StoreMeetingRequest` — authorize: `$this->user()->can('create', Meeting::class)`. Rules: title (required, string, max:255), type (required, in:review,advisory,tax_planning,onboarding,general), starts_at (required, date), ends_at (required, date, after:starts_at), contact_id (nullable, integer, exists:contacts,id), location (nullable, string, max:500), description (nullable, string). After hook: validate starts_at and ends_at are same calendar date. File: `app/Http/Requests/Meeting/StoreMeetingRequest.php`
- [ ] T014 FormRequest: `UpdateMeetingRequest` — authorize: pre-load Meeting via uuid route param + workspace_id, stash on `$this->attributes->set('meeting', $meeting)`, return `$this->user()->can('update', $meeting)`. Rules: same as Store but all `sometimes`. Same-day validation in after hook. File: `app/Http/Requests/Meeting/UpdateMeetingRequest.php`
- [ ] T015 FormRequest: `CompleteMeetingRequest` — authorize: pre-load Meeting via uuid, stash on attributes, return `$this->user()->can('update', $meeting)`. No rules (no body needed). File: `app/Http/Requests/Meeting/CompleteMeetingRequest.php`
- [ ] T016 Action: `CreateMeeting` — `use AsAction`. `handle(int $workspaceId, int $userId, array $validated): Meeting`. Generate UUID, create Meeting with workspace_id, created_by, status=scheduled. Auto-create MeetingAttendee for creator (user_id=$userId, is_organiser=true, rsvp_status=accepted, invited_at=now). Return fresh meeting with attendees loaded. File: `app/Actions/Meeting/CreateMeeting.php`
- [ ] T017 Action: `UpdateMeeting` — `use AsAction`. `handle(Meeting $meeting, array $validated): Meeting`. Detect if starts_at or ends_at changed (reschedule). Update meeting fields. If rescheduled, queue `MeetingRescheduledMail` to all non-organiser attendees via `Mail::queue()`. Return fresh meeting. File: `app/Actions/Meeting/UpdateMeeting.php`
- [ ] T018 Action: `MarkMeetingComplete` — `use AsAction`. `handle(Meeting $meeting): Meeting`. Set status=completed, completed_at=now(). If meeting has recurring_template_id, call `$meeting->recurringTemplate->advanceToNextDueDate()` and trigger `ProcessRecurringTemplate::run()` for next occurrence. Return fresh meeting. File: `app/Actions/Meeting/MarkMeetingComplete.php`
- [ ] T019 Action: `CancelMeeting` — `use AsAction`. `handle(Meeting $meeting): Meeting`. Set status=cancelled, cancelled_at=now(). Queue `MeetingCancelledMail` to all attendees via `Mail::queue()`. Soft delete the meeting (`$meeting->delete()`). Return meeting. File: `app/Actions/Meeting/CancelMeeting.php`
- [ ] T020 Resource: `MeetingResource` — fields: id, uuid, title, type (with label), starts_at (ISO8601), ends_at (ISO8601), contact (whenLoaded, ContactResource), location, description, status (with label), created_by, creator (whenLoaded), completed_at, cancelled_at, recurring_template_id, attendees (whenLoaded, MeetingAttendeeResource::collection), attendees_count (whenCounted), notes_count (whenCounted), attachments_count (whenCounted), created_at, updated_at. File: `app/Http/Resources/MeetingResource.php`
- [ ] T021 [P] Resource: `MeetingAttendeeResource` — fields: id, user_id, user (whenLoaded — id, name, email), email, name, rsvp_status (with label), is_organiser, invited_at, responded_at. File: `app/Http/Resources/MeetingAttendeeResource.php`
- [ ] T022 Controller: `MeetingController` — `index(Request)`: Gate::authorize viewAny, query by workspace_id with optional status filter + search (title, contact name, location), paginate(25), return MeetingResource::collection. `counts(Request)`: Gate::authorize viewAny, GROUP BY status, return JSON. `store(StoreMeetingRequest)`: call CreateMeeting::run, return MeetingResource 201. `show(Request, string $uuid)`: find by uuid+workspace_id, load attendees+contact+creator, withCount notes+attachments, Gate::authorize view, return MeetingResource. `update(UpdateMeetingRequest)`: get meeting from attributes, call UpdateMeeting::run, return MeetingResource. `destroy(Request, string $uuid)`: find by uuid+workspace_id, Gate::authorize delete, call CancelMeeting::run, return message JSON. `complete(CompleteMeetingRequest)`: get meeting from attributes, call MarkMeetingComplete::run, return MeetingResource. File: `app/Http/Controllers/Api/MeetingController.php`
- [ ] T023 Routes: Add meeting routes inside workspace-scoped middleware group in `api.php`. Order: `GET meetings` (index), `GET meetings/counts` (BEFORE uuid routes), `POST meetings` (store), `GET meetings/{uuid}` (show), `PATCH meetings/{uuid}` (update), `DELETE meetings/{uuid}` (destroy), `POST meetings/{uuid}/complete` (complete). Add `meetings` to the polymorphic `parentType` regex for attachment and note routes. File: `routes/api.php`
- [ ] T024 Tests: Meeting CRUD — test index (paginated, filtered by status, searched by title), test store (valid data creates meeting + organiser attendee, returns 201), test show (loads attendees, contact, counts), test update (changes fields, returns updated), test destroy (sets cancelled, soft deletes), test complete (sets completed, completed_at set), test permission denial (client cannot create, auditor cannot update), test tenant isolation (workspace A user cannot see workspace B meetings). File: `tests/Feature/Api/MeetingTest.php`

---

## Phase 3: Attendees & Email

- [ ] T025 Action: `AddMeetingAttendee` — `use AsAction`. `handle(Meeting $meeting, ?int $userId, ?string $email, ?string $name): MeetingAttendee`. Create attendee with rsvp_status=pending, invited_at=now. Queue `MeetingInvitationMail` via `Mail::queue()` — for workspace users send to user email, for external send to provided email. Generate signed RSVP URLs: `URL::signedRoute('meeting.rsvp', ['attendee' => $attendee->id, 'response' => 'accepted'])` and same for declined. Pass URLs to mailable. Return fresh attendee. File: `app/Actions/Meeting/AddMeetingAttendee.php`
- [ ] T026 FormRequest: `StoreAttendeeRequest` — authorize: pre-load meeting by uuid, `$this->user()->can('update', $meeting)`. Rules: user_id (nullable, integer, exists:users,id), email (nullable, required_without:user_id, email, max:255), name (nullable, string, max:255). After hook: validate uniqueness — no duplicate (meeting_id, user_id) or (meeting_id, email). File: `app/Http/Requests/MeetingAttendee/StoreAttendeeRequest.php`
- [ ] T027 Mailable: `MeetingInvitationMail` — constructor: $meetingTitle, $meetingDate, $meetingTime, $meetingLocation, $organiserName, $acceptUrl, $declineUrl. Subject: "Meeting Invitation: {$meetingTitle}". View: `mail.meeting-invitation`. File: `app/Mail/MeetingInvitationMail.php`
- [ ] T028 [P] Mailable: `MeetingRescheduledMail` — constructor: $meetingTitle, $oldDate, $oldTime, $newDate, $newTime, $meetingLocation, $organiserName. Subject: "Meeting Rescheduled: {$meetingTitle}". View: `mail.meeting-rescheduled`. File: `app/Mail/MeetingRescheduledMail.php`
- [ ] T029 [P] Mailable: `MeetingCancelledMail` — constructor: $meetingTitle, $meetingDate, $meetingTime, $organiserName. Subject: "Meeting Cancelled: {$meetingTitle}". View: `mail.meeting-cancelled`. File: `app/Mail/MeetingCancelledMail.php`
- [ ] T030 [P] Blade email templates — 3 files: `meeting-invitation.blade.php` (title, date, time, location, organiser, accept/decline buttons with signed URLs), `meeting-rescheduled.blade.php` (title, old date/time, new date/time, location), `meeting-cancelled.blade.php` (title, date/time, organiser). Follow existing email template style from `resources/views/mail/`. Files: `resources/views/mail/meeting-invitation.blade.php`, `resources/views/mail/meeting-rescheduled.blade.php`, `resources/views/mail/meeting-cancelled.blade.php`
- [ ] T031 Controller: `MeetingAttendeeController` — `index(Request, string $uuid)`: find meeting by uuid+workspace_id, Gate::authorize view meeting, return MeetingAttendeeResource::collection of meeting->attendees with user loaded. `store(StoreAttendeeRequest)`: get meeting from attributes, call AddMeetingAttendee::run, return MeetingAttendeeResource 201. `destroy(Request, string $uuid, int $attendeeId)`: find meeting, find attendee, Gate::authorize update meeting, delete attendee, return message JSON. File: `app/Http/Controllers/Api/MeetingAttendeeController.php`
- [ ] T032 RSVP Controller: `MeetingRsvpController` — single `__invoke(Request $request)` method. Read `attendee` and `response` from query params. Find MeetingAttendee by id. Validate response is 'accepted' or 'declined'. Update rsvp_status and responded_at=now(). Return JSON `{ message: 'Response recorded', status: $response }`. File: `app/Http/Controllers/Api/MeetingRsvpController.php`
- [ ] T033 Routes: Add attendee routes inside workspace-scoped group: `GET meetings/{uuid}/attendees`, `POST meetings/{uuid}/attendees`, `DELETE meetings/{uuid}/attendees/{attendeeId}`. Add public RSVP route OUTSIDE auth middleware: `GET /api/v1/meetings/rsvp` with `->middleware('signed')->name('meeting.rsvp')` using MeetingRsvpController. File: `routes/api.php`
- [ ] T034 Tests: Attendees & Email — test add workspace user attendee (creates attendee, queues MeetingInvitationMail), test add external email attendee (creates attendee, queues invitation), test remove attendee, test duplicate attendee rejected (unique constraint), test RSVP accept via signed URL (updates rsvp_status to accepted, responded_at set), test RSVP decline, test unsigned URL rejected (403), test reschedule triggers MeetingRescheduledMail to all non-organiser attendees (use Mail::fake()), test cancel triggers MeetingCancelledMail to all attendees. File: `tests/Feature/Api/MeetingAttendeeTest.php`

---

## Phase 4: Calendar Aggregation API

- [ ] T035 Resource: `CalendarEventResource` — fields: type (string — event type slug), date (Y-m-d), starts_at (ISO8601, nullable — only for meetings), ends_at (ISO8601, nullable — only for meetings), label (string), amount (int, nullable — only for invoices/bills), contact_name (string, nullable), source_type (string — morph alias), source_uuid (string), colour (string — green/red/orange/purple/blue/grey/teal). File: `app/Http/Resources/CalendarEventResource.php`
- [ ] T036 Controller: `CalendarController` — `events(Request $request)`: validate start_date + end_date required (date format). Build 5 separate queries conditionally based on user permissions: (1) Invoices: `Invoice::where('workspace_id', ...)->where('type', 'invoice')->whereIn('status', ['approved','sent','partial'])->whereBetween('due_date', [$start, $end])->get()->map()` — if due_date->isPast() type='invoice-overdue' colour='red', else type='invoice-due' colour='green'. (2) Bills: same pattern with type='bill', colour orange/red. (3) RecurringTemplates: active templates with next_due_date in range, type='recurring-entry', colour='purple'. (4) AccountingPeriods: start_date/end_date/lock_date in range, 3 event types (period-open blue, period-close blue, period-lock grey). (5) Meetings: scheduled+completed where starts_at in range, type='meeting', colour='teal'. Merge all collections, sortBy date, return CalendarEventResource::collection. `upcoming(Request $request)`: same 5 queries but for next 14 days from today, return flat chronological list. File: `app/Http/Controllers/Api/CalendarController.php`
- [ ] T037 Routes: Add calendar routes inside workspace-scoped group: `GET calendar/events` (CalendarController@events), `GET calendar/upcoming` (CalendarController@upcoming). File: `routes/api.php`
- [ ] T038 Update `ProcessRecurringTemplate` — add `Meeting` case to the type switch. Add `processMeeting()` private method: extract template_data fields, derive starts_at from next_due_date + time_of_day, derive ends_at from starts_at + duration_minutes. Call `CreateMeeting::run()` with extracted data. Handle attendee_user_ids and attendee_emails by calling `AddMeetingAttendee::run()` for each. Update return type to `Invoice|JournalEntry|Meeting`. File: `app/Actions/Recurring/ProcessRecurringTemplate.php`
- [ ] T039 Tests: Calendar aggregation — test events endpoint returns invoices on correct dates with green colour, test overdue invoices return red, test bills on correct dates with orange, test overdue bills return red, test recurring templates on next_due_date with purple, test period open/close/lock dates with blue/grey, test meetings on starts_at with teal, test completed meetings still shown, test paid invoices excluded, test cancelled meetings excluded, test paused templates excluded, test date range filtering (events outside range not returned), test permission-based filtering (client with no invoice.view permission doesn't see invoices), test tenant isolation, test upcoming returns next 14 days chronologically. File: `tests/Feature/Api/CalendarTest.php`
- [ ] T040 Update DemoPersonasSeeder — add 3-4 sample meetings: 1 scheduled (future, with 2 attendees), 1 completed (past, with notes), 1 cancelled (past). Link to existing demo contacts. Add as workspace-scoped data for admin@moneyquest.app workspace. File: `database/seeders/DemoPersonasSeeder.php`

---

## Phase 5: Frontend — Calendar Page

- [ ] T041 [P] Types: `CalendarEvent` and `CalendarEventType` — type CalendarEvent = { type: CalendarEventType, date: string, starts_at: string | null, ends_at: string | null, label: string, amount: number | null, contact_name: string | null, source_type: string, source_uuid: string, colour: string }. type CalendarEventType = 'invoice-due' | 'invoice-overdue' | 'bill-due' | 'bill-overdue' | 'recurring-entry' | 'period-open' | 'period-close' | 'period-lock' | 'meeting'. File: `frontend/src/types/calendar.ts`
- [ ] T042 [P] Types: `Meeting`, `MeetingAttendee`, `MeetingType`, `MeetingStatus`, `AttendeeRsvpStatus` — matching API resource shapes. Include nested Contact type for meeting.contact. File: `frontend/src/types/meeting.ts`
- [ ] T043 Hook: `useCalendarEvents(startDate: string, endDate: string)` — TanStack Query, queryKey: ['calendar', 'events', startDate, endDate], GET /calendar/events with start_date/end_date params, staleTime: 5 * 60 * 1000. `useUpcomingEvents()` — queryKey: ['calendar', 'upcoming'], GET /calendar/upcoming, staleTime: 5 * 60 * 1000. File: `frontend/src/hooks/use-calendar.ts`
- [ ] T044 Store: `calendar-store` — Zustand store. State: viewMode ('month' | 'week'), currentDate (Date — the anchor date for the view), filters (Record<CalendarEventType, boolean> — all true by default), selectedDate (Date | null). Actions: setViewMode, setCurrentDate, toggleFilter, clearFilters, setSelectedDate, goToToday, goNext (add 1 month or 1 week), goPrev (subtract 1 month or 1 week). File: `frontend/src/stores/calendar-store.ts`
- [ ] T045 Component: `CalendarNav` — prev/next buttons (ChevronLeft/ChevronRight icons), Today button, current month/year or week range label, month/week toggle (shadcn ToggleGroup). Props: viewMode, currentDate, onViewModeChange, onNavigate, onToday. File: `frontend/src/components/calendar/CalendarNav.tsx`
- [ ] T046 Component: `EventFilters` — row of toggleable shadcn Badge/Chip components, one per event type. Each shows colour dot + label. Active = filled, inactive = outlined. "Clear filters" button when any deselected. If last filter deselected → reset all to active (FR-022). Props: filters (Record<CalendarEventType, boolean>), onToggle, onClear. File: `frontend/src/components/calendar/EventFilters.tsx`
- [ ] T047 Component: `EventIndicator` — small colour-coded dot (month view) or labelled pill (week view). Props: colour (string), label (string), variant ('dot' | 'pill'), time (string | null — for week view pill positioning). File: `frontend/src/components/calendar/EventIndicator.tsx`
- [ ] T048 Component: `EventPopover` — shadcn Popover triggered by clicking an event indicator. Shows: type badge (colour dot + label), record name/title, amount (for invoices/bills, formatted via formatMoney), contact name, date/time, "View [record type]" link (navigates to /invoices/{uuid}, /bills/{uuid}, /meetings/{uuid}, /settings/accounting-periods). For meetings: also show "Edit" and "Cancel" buttons. Props: events (CalendarEvent[]), onClose, onNavigate. File: `frontend/src/components/calendar/EventPopover.tsx`
- [ ] T049 Component: `DayCell` — individual day cell in month grid. Shows date number (today highlighted with ring/bg), up to 4 EventIndicator dots, "+N more" label if > 4 events. Click on cell → setSelectedDate. Click on indicator → open EventPopover. Grey out days outside current month. Props: date (Date), events (CalendarEvent[]), isToday, isCurrentMonth, onDateClick, onEventClick. File: `frontend/src/components/calendar/DayCell.tsx`
- [ ] T050 Component: `MonthView` — CSS Grid 7-column layout. Header row: Mon-Sun labels. 6 rows of DayCell components. Use date-fns `startOfMonth`, `startOfWeek`, `eachDayOfInterval`, `endOfWeek`, `endOfMonth` to compute the 42-day grid. Filter events by the calendar store filters before passing to cells. Props: currentDate, events, filters. File: `frontend/src/components/calendar/MonthView.tsx`
- [ ] T051 Component: `WeekView` — 7 day columns. Header row: day names + dates. Financial events (date-only) rendered at top of column as pills. Meeting events rendered time-positioned using starts_at hour (e.g. top = 8am, each hour = fixed height). Scrollable vertically. Events as EventIndicator variant='pill' with time label. Props: currentDate, events, filters. File: `frontend/src/components/calendar/WeekView.tsx`
- [ ] T052 Component: `DayDetailPanel` — shadcn Sheet (side panel) showing all events for a selected day. Triggered when clicking "+N more" or a date cell. Lists events grouped by type with full details. Each event clickable → EventPopover. Props: date (Date), events (CalendarEvent[]), open, onClose. File: `frontend/src/components/calendar/DayDetailPanel.tsx`
- [ ] T053 Component: `UpcomingSidebar` — right sidebar panel showing next 14 days. Events grouped by date heading ("Today", "Tomorrow", "Fri 21 Mar"). Each entry: colour badge + label + amount (if applicable). Empty state: "No upcoming financial events in the next 14 days." Uses useUpcomingEvents() hook. Clickable events → EventPopover. File: `frontend/src/components/calendar/UpcomingSidebar.tsx`
- [ ] T054 Component: `CalendarView` — main container. Layout: CalendarNav + EventFilters at top, MonthView or WeekView in center, UpcomingSidebar on right. Reads viewMode/currentDate/filters from calendar-store. Computes start_date/end_date with 7-day buffer for useCalendarEvents hook. Handles DayDetailPanel open/close. File: `frontend/src/components/calendar/CalendarView.tsx`
- [ ] T055 Page: `/calendar` — 'use client'. Renders CalendarView. Page title "Financial Calendar". Full-width layout (no sidebar collapse needed — sidebar stays). File: `frontend/src/app/(dashboard)/calendar/page.tsx`
- [ ] T056 Update sidebar — add Calendar nav item: icon=Calendar (lucide-react), label="Calendar", href="/calendar", shortcut="G then Y". Add Meetings nav item: icon=Users (lucide-react), label="Meetings", href="/meetings", shortcut="G then E". File: `frontend/src/components/layout/sidebar.tsx`
- [ ] T057 Update navigation — add `/calendar` and `/meetings` to route definitions and `urlToModule` mapping. File: `frontend/src/lib/navigation.ts`
- [ ] T058 Update shortcuts store — add `G then Y` → navigate to /calendar, `G then E` → navigate to /meetings. File: `frontend/src/stores/shortcuts-store.ts`

---

## Phase 6: Frontend — Meetings Pages

- [ ] T059 Hook: `useMeetings(params)` — TanStack Query, queryKey: ['meetings', params], GET /meetings with page/per_page/status/search params. `useMeeting(uuid)` — GET /meetings/{uuid}. `useMeetingCounts()` — GET /meetings/counts. `useCreateMeeting()` — useMutation POST /meetings, invalidate ['meetings']. `useUpdateMeeting()` — useMutation PATCH /meetings/{uuid}, invalidate ['meetings']. `useMarkComplete(uuid)` — useMutation POST /meetings/{uuid}/complete, invalidate ['meetings', 'calendar']. `useCancelMeeting(uuid)` — useMutation DELETE /meetings/{uuid}, invalidate ['meetings', 'calendar']. `useAddAttendee(uuid)` — useMutation POST /meetings/{uuid}/attendees. `useRemoveAttendee(uuid)` — useMutation DELETE /meetings/{uuid}/attendees/{id}. File: `frontend/src/hooks/use-meetings.ts`
- [ ] T060 Component: `MeetingStatusBadge` — shadcn Badge. Scheduled=default (slate), Completed=success (green), Cancelled=destructive (red). Props: status (MeetingStatus). File: `frontend/src/components/meetings/MeetingStatusBadge.tsx`
- [ ] T061 Component: `MeetingForm` — React Hook Form + Zod schema. Fields: title (Input, required, max 255), type (Select with MeetingType options), starts_at (date picker + time input, required), ends_at (time input, required, after starts_at, same day validation), contact_id (Contact search/select, optional), location (Input, optional, max 500), description (Textarea, optional). Submit calls useCreateMeeting or useUpdateMeeting. Pre-fill date when created from calendar click. Props: meeting (Meeting | undefined — for edit mode), defaultDate (string | undefined — from calendar), onSuccess. File: `frontend/src/components/meetings/MeetingForm.tsx`
- [ ] T062 Component: `AttendeeList` — list of attendees with columns: name/email, type (workspace user vs external), RSVP status badge (pending=yellow, accepted=green, declined=red), organiser badge, remove button (X icon, calls useRemoveAttendee). Props: attendees (MeetingAttendee[]), meetingUuid, canEdit. File: `frontend/src/components/meetings/AttendeeList.tsx`
- [ ] T063 Component: `AddAttendeeForm` — two modes: (1) workspace user — searchable Select of workspace members, (2) external — email Input + name Input. Toggle between modes. Submit calls useAddAttendee. Props: meetingUuid, onAdded. File: `frontend/src/components/meetings/AddAttendeeForm.tsx`
- [ ] T064 Page: `/meetings` — 'use client'. StatusTabs (scheduled/completed/cancelled) using useMeetingCounts. DataTable with columns: title, type (badge), date/time, contact name, attendees count, status badge. Search input filtering by title/contact/location. Row click → navigate to /meetings/{uuid}. "New Meeting" button (top right) with kbd hint "N". File: `frontend/src/app/(dashboard)/meetings/page.tsx`
- [ ] T065 Page: `/meetings/new` — 'use client'. Renders MeetingForm in create mode. Read `date` from searchParams (pre-fill from calendar click). On success → navigate to /meetings/{uuid}. File: `frontend/src/app/(dashboard)/meetings/new/page.tsx`
- [ ] T066 Page: `/meetings/[id]` — 'use client'. Meeting detail page. Header: title + MeetingStatusBadge + type badge. Info section: date/time, location (clickable if URL), contact (link to /contacts/{id}), creator name. Action buttons: "Edit" (navigate to edit page), "Mark Complete" (if scheduled, calls useMarkComplete), "Cancel" (if scheduled, AlertDialog confirmation, calls useCancelMeeting). Attendee section: AttendeeList + AddAttendeeForm (if canEdit). Notes section: existing notes component (TipTap). Attachments section: existing attachments component (drag-drop). File: `frontend/src/app/(dashboard)/meetings/[id]/page.tsx`
- [ ] T067 Page: `/meetings/[id]/edit` — 'use client'. Renders MeetingForm in edit mode, pre-filled with meeting data from useMeeting hook. On success → navigate back to /meetings/{uuid}. File: `frontend/src/app/(dashboard)/meetings/[id]/edit/page.tsx`

---

## Phase 7: Browser Tests & Polish

- [ ] T068 Browser test: Calendar page — login as admin, navigate to /calendar, verify month grid renders with today highlighted, verify at least one event indicator visible (from seeded data), click forward arrow → next month renders, click "Today" → returns to current month, switch to week view → week columns render. File: `tests/Browser/CalendarTest.php`
- [ ] T069 Browser test: Meetings CRUD — login as admin, navigate to /meetings, verify StatusTabs render with counts, click "New Meeting", fill title + type + date + time, submit → redirected to detail page, verify meeting appears in list. Navigate to detail page, click "Mark Complete" → status changes to completed. File: `tests/Browser/MeetingTest.php`
- [ ] T070 Run `vendor/bin/pint --dirty` on all new/modified PHP files. Verify `php artisan test --compact` passes all tests. File: all modified PHP files.
